# Ignore macOS system files
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Ignore sensitive files and Docker volumes
.env                  # Environment variables (do NOT commit secrets)
env/                  # Any other env-related directories
.n8n                  # n8n workflows and persistent data
postgres-data/        # PostgreSQL data volume

# Ignore temporary or large files
*.tar.gz              # Backup archives
*.sql.gz              # Database backups
tmp/                  # Temporary directory
charts/               # Chart files (if generated and not part of the source)
summaries/            # Summaries (if generated and not part of the source)

# Node.js (if applicable)
node_modules/         # Dependencies for JavaScript/Node.js (if you ever add them)
npm-debug.log*        # npm debug log files
yarn-error.log*       # Yarn error log files
